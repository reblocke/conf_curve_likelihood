[project]
name = "confcurve"
version = "0.2.5"
description = "Static GitHub Pages app and compatibility adapter for Wald inference"
readme = "README.md"
requires-python = ">=3.11"
license = {file = "LICENSE"}
authors = [
  {name = "Brian Locke"},
]
dependencies = [
  "numpy>=2.2.5,<2.3",
  "scipy>=1.14.1,<1.15",
  "wald-inference @ https://github.com/reblocke/wald-inference-core/releases/download/v0.4.1/wald_inference-0.4.1-py3-none-any.whl#sha256=d7272023f65088729d3ff997cab7cac57b84f22ac6108244ec2170434557d99b",
]

[dependency-groups]
dev = [
  "pytest==9.0.3",
  "hypothesis>=6.151,<7",
  "idna==3.15",
  "playwright>=1.55,<2",
  "pygments==2.20.0",
  "pytest-playwright>=0.7,<1",
  "requests==2.33.0",
  "ruff>=0.15.1",
  "pre-commit>=3.7",
  "urllib3==2.7.0",
]

[tool.pytest.ini_options]
minversion = "8.0"
addopts = ["-q"]
testpaths = ["tests"]
markers = [
  "e2e: browser end-to-end tests",
]

[tool.ruff]
line-length = 100
target-version = "py311"
src = ["src", "tests", "scripts"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.ruff.lint.isort]
known-first-party = ["confcurve"]


[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
